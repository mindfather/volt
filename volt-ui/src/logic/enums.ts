export enum Pokes {
  Command = "volt-command",
  Action = "volt-action",
  Message = "volt-message",
};

export enum Networks {
  Mainnet = "mainnet",
  Testnet = "testnet",
  Regtest = "regtest",
}
